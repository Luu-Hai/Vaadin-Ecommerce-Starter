server.port=${PORT:8082}
logging.level.org.atmosphere = warn

# Launch the default browser when starting the application in development mode
vaadin.launch-browser=true
# To improve the performance during development.
# For more information https://vaadin.com/docs/latest/integrations/spring/configuration#special-configuration-parameters
vaadin.allowed-packages = com.vaadin,org.vaadin,com.lcaohoanq
spring.jpa.defer-datasource-initialization = true

spring.application.name=Spring-Snake-Game

# API
v1API=/api/v1

# MySQL database configuration
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://localhost:3310/mysql_starter}
spring.datasource.username=root
spring.datasource.password=${MYSQL_ROOT_PASSWORD:Luucaohoang1604^^}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=create-drop
spring.jpa.show-sql=false